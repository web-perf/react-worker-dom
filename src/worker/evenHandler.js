import Bridge from './bridge';

class EventHandler {
    constructor() {
        super();
        this.eventHandlers = {};
    }

    add(node, type, handler, useCapture) {
        if (node.)
    }

    remove(node, type, handler) {

    }

    onEventHandler(node, type, e) {
        console.log('Event Handler invoked from ', node, type);
    }
}

export default new EventHandler();