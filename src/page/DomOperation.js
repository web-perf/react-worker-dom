import {OPS as _} from './../../common/constants';

export default class DomOperation {
    constructor(container) {
        this.container = container;
        this.nodes = {};
    }
    handleDomOperation(op, guid, args) {
        if (op === _.attachRoot){
            this.container
        }
    }
}

const operations = {

}